NOTAS PRA MENTORIA:

SEMANA 1

Pra relembrar:
* .venv é o virtual enviroment do python pra não instalar as dependencias globalmente
* deve ser criado via linha de comando no terminal
* requirements é equivalente ao package.json do node
* separei as responsas seguindo a lógica:
  - api pras rotas
  - schemas pra validação de dados/contratos
  - service pra lógica da aplicação (regras de negocio)
  - core pras configs (vai ter coisa no futuro)

Perguntas:
* Service deveria retornar string ou um obj?
* Em que momento faz sentido trocar função por classe no service?
* Essa separação API/service/schema tá correta?

TODO: fazer uma branch dev quqando começar a semana 2

SEMANA 2

Pra relembrar:
* adicionando a pasta repositories pra cuidar das configs do banco e da lógica com SLQAlchemy
* adicionando a pasta models pra cuidar da representação da tabela no banco e dados persistidos
* lambda = função anônima com sintaxe reduzida do python / usada aqui pra adiar execução do datetime
* repository existe para isolar persistência/acesso ao banco
* model != schema
  - model representa tabela/banco
  - schema representa entrada/saída da API
* fluxo atual:
  api -> service -> repository -> database
* database.py virou um hub de infra:
  - engine
  - SessionLocal
  - Base
  - get_db
* SessionLocal é fábrica de sessões, não a sessão em si
* Session = "conversa ativa" com o banco
* FastAPI usa Depends + get_db para injetar sessão automaticamente
* yield no get_db permite setup + cleanup automático
* service agora faz alguma coisa pertinente
* SQLite criou o banco automaticamente via SQLAlchemy + create_all

//////////// Continuando a semana 2 (a saga 🧙‍♂️)
* git rm --cached nomeDoArquivo <- comando pra remover arquivo do rastreamento do git sem deletar!
* criando o config.py pra cuidar das env vars
* criando a pasta tests para os testes unitarios e pytest instalado
* criando requirements-dev só com as dependencias de desenvolvimento (por enquanto só pytest rs)


Perguntas:
* vale usar SQLite até Docker/Postgres ou não?
* a responsabilidade de cada camada está boa/clara?

//////////// Continuando a semana 2 (a saga 🧙‍♂️)
* API: response_model no decorator ou usar como tipo?


SEMANA 3

Pra relembrar:

* Imagem = molde do container
* Container = instância em execução da imagem
* PostgreSQL é um serviço separado da aplicação, diferente do sqlite que é só um arquivo
* DATABASE_URL permitiu trocar sqlite por postgresql sem alterar API, service ou repository (delícia de arquitetura)
* Variáveis passadas com -e viram env vars dentro do container
* docker exec permite executar comandos dentro de um container já rodando
* psql é o cliente de terminal do postgresql
* \dt lista as tabelas do banco
* \d nome_tabela mostra a estrutura da tabela
* reviews_db=# indica que estou dentro do postgresql
* Dockerfile descreve como construir uma imagem
* docker build gera uma imagem
* docker run cria um container a partir da imagem
* .dockerignore impede de subir junk pro pra imagem
* Docker Compose orquestra múltiplos containers! (belo)
* localhost dentro do container != localhost da máquina
* depends_on controla ordem de inicialização


Perguntas:

* foi um bom momento pra migrar pro postgresql?
* dockerfile é tipo uma receita pra imagens, e o compose pra subir multiplas imagems, é isso msm?
* quando a gente fala de "serviço funcionando" em docker, seria o container rodando pleno?
* database url direto no compose é de boas?
* depends_on ou healthcheck?
* em que momento entra volumes? como usa-los?
* o que vc mudaria nessa arquitetura do docker?

FIX SEMANA 3

* agora camada de api tá completamente desacoplada do service
* testes também foram ajeitados de acordo com essa nova mudança no service
* adicionado o volume para o banco, e o healthcheck no compose
* escondendo a url do banco no .env


SEMANA 4

* novo serviço no compose para o ollama
* novo serviço no compose para baixar os modelos do ollama
* criada a camada de providers com uma classe abstrata llm provider
* adicionei nas settings e no .env ollama url e model
* instalei pacote de chamadas http
* testei o ollama isolado via insomnia antes de integrar na API
* testei o fluxo completo pelo swagger
* adicionei timeout maior na chamada HTTP porque o ollama teve cold start carregando o modelo
* qwen2.5-coder:7b rodando localmente via docker ✨

Perguntas

* no exemplo que mostrou na nossa call vc estava usando network_mode: host, qual a implicação disso?
* o timeout alto no OllamaProvider é uma solução aceitável por enquanto?
* no service, a factory ta criando um provider novo a cada request, isso daria ruim em um projeto grande né? de que forma seria mitigado?
* qual a forma de ideal de incorporar na arquitetura uma camada RAG?
* que tipo de fine tuning poderia ser implementado nesse projeto? (Few-shot Learning, LoRA, QLoRA)
* queria entender melhor como seria a implementação de um banco vetorial com embeddings em um projeto como esse
* chunking? indexação?
* usar a VRAM pra rodar modelos?

SEMANA 5

* retorno corrigido no service
* tenacity adicionado no ollama provider, timeout diminuiu pra 60 sec
* logging_config adicionado
* logging_config no lifespan, não no nível do módulo, lifespan roda depois do Uvicorn e a nossa config prevalece
* endpoint /health verifica Ollama e banco antes de responder, retorna 503 quando unhealthy, permite integração com Docker healthcheck
* healthcheck do compose agora aponta pra /health da API

Perguntas:

* Essa arquitetura que construímos de Router + Service + Repository seria a mais comum em projetos de Python/FastAPI reais?
* Quais outras arquiteturas ou padrões você acha que eu deveria estudar depois dessa mentoria? Clean Architecture, Hexagonal, DDD etc.? Qual seria a ordem ideal?
* Como se identifica que estou "arquitetando" muito uma aplicação, qual a linha entre overengineering e boa arquitetura?
* Se o projeto crescesse pra vários serviços, vários providers de IA e muitos usuários simultâneos, que tipo de modificação teriamos que fazer na arquitetura?
* Que conceitos de backend/arquitetura você acha que deveriam ser meus próximos focos de estudo?
* Quais seriam os próximos passos pra eu sair de "sei implementar essa arquitetura" pra "sei decidir qual arquitetura usar"?